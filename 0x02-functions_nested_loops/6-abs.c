#include <stdio.h>
#include "main.h"

/**
 * _abs - computes absolute value of integer
 * @n: provided integer
 * Return: return absolute value of integer
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (n);
	}

	else
	{
		return (n);

	}

	return (0);
}
