#include "main.h"

/**
* _isupper - checks for uppercase letter
* Returns - Returns (1) if uppercase or (0) if lowercase
*/

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);

	return ('\n');
}
